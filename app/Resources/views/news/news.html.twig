{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for news in newsList %}
            <div class="well">
                <h3><span>{{ news.head }}</span></h3>
                <p>{{ news.body }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}
