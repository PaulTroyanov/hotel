{% extends 'base.html.twig' %}

{% block body %}
    {% for flash_message in app.session.flashbag.get('notice') %}
        <div class="alert alert-success" style="text-align: center;">
            {{ flash_message }}
        </div>
    {% endfor %}

    <div class="well">
        <h1 style="text-align: center;"><span>Create news</span></h1>
        {{ form(form) }}
    </div>
{% endblock %}
